# Story Points Poker - Product Requirements Document

## Обзор проекта

Веб-приложение для Planning Poker (Story Points estimation) для команды. Замена тормозному и перегруженному рекламой scrumpoker-online.org.

**Цель:** Создать максимально простой и быстрый инструмент для оценки Story Points в команде из ~8 человек, с сессиями раз в 2 недели по ~2 часа.

---

## Технический стек

- **Frontend:** Next.js 14 (App Router)
- **UI/Styling:** shadcn/ui + Tailwind CSS + next-themes (dark/light mode)
- **Backend/Storage:** Vercel KV (Redis)
- **Real-time:** Simple polling (каждые 2-3 секунды)
- **Hosting:** Vercel (бесплатный plan)

---

## Основной функционал

### 1. Главная страница (`/`)

**Элементы:**
- Поле ввода "Your name"
- Кнопка "Create room"
- Light/Dark theme toggle (справа вверху)

**Поведение:**
- Пользователь вводит имя
- Нажимает "Create room"
- Создается комната с рандомным ID (например: `abc123`)
- Редирект на `/room/[id]`
- Пользователь становится создателем (модератором) комнаты

### 2. Вход в существующую комнату

**Флоу:**
- Пользователь получает ссылку от создателя (например: `yoursite.vercel.app/room/abc123`)
- Переходит по ссылке
- Видит форму "Your name"
- Вводит имя и входит в комнату
- Становится обычным участником

**Edge cases:**
- Если комната удалена/истекла → показать страницу "Room expired" с кнопкой "Go to home"

### 3. Страница комнаты (`/room/[id]`)

#### UI Элементы:

**Header:**
- Room ID (например: "Room: abc123")
- Кнопка "Copy link" (копирует полную ссылку в буфер)
- Light/Dark theme toggle

**Список участников:**
Показывает всех участников в реальном времени со статусами:
- Не проголосовал: `👤 Name`
- Проголосовал (но результаты скрыты): `✓ Name`
- Результаты показаны: `Name: 5` (крупная карточка со значением)

**Карточки для голосования:**
Крупные, кликабельные карточки с значениями:
- `1, 2, 3, 5, 8, 13, 21, ?, ☕️`
- Активная карточка подсвечивается
- После выбора сразу отправляется голос

**Контролы (только для создателя комнаты):**
- Кнопка "Show estimates" / "Hide estimates" (toggle, одна кнопка меняет текст)
  - По дефолту: "Show estimates"
  - После нажатия: "Hide estimates"
  - Все участники видят результаты голосования
- Кнопка "Delete estimates" (отдельная кнопка)
  - Сбрасывает все голоса
  - Начинается новый раунд

#### Поведение:

**Участник:**
1. Видит карточки
2. Выбирает значение
3. У него подсвечивается выбранная карточка
4. У других участников появляется галочка "✓" возле его имени
5. Ждет пока модератор нажмет "Show estimates"
6. Видит результаты всех

**Модератор (создатель):**
1. Делает все то же что и участник
2. Дополнительно может:
   - Показать результаты всем ("Show estimates")
   - Скрыть результаты обратно ("Hide estimates")
   - Удалить все голоса и начать новый раунд ("Delete estimates")

**Real-time обновления:**
- Polling каждые 2-3 секунды
- Автоматическое обновление списка участников
- Автоматическое обновление статусов голосования
- Автоматическое обновление результатов

---

## Данные и хранение

### Структура данных в Redis (Vercel KV):

```typescript
// Ключ: room:{roomId}
{
  id: string;                    // abc123
  creatorId: string;             // user123 (уникальный ID создателя)
  createdAt: number;             // timestamp
  lastActivity: number;          // timestamp для TTL
  revealed: boolean;             // показаны ли результаты
  participants: {
    [userId: string]: {
      name: string;              // имя участника
      vote: string | null;       // выбранное значение или null
      votedAt: number | null;    // когда проголосовал
      isCreator: boolean;        // флаг модератора
    }
  }
}
```

### TTL (Time To Live):
- Комната автоматически удаляется через **3 часа** неактивности
- Redis TTL обновляется при каждом действии в комнате

---

## Правила и Edge Cases

### 1. Создатель покинул комнату
- Права модератора передаются первому участнику (по порядку входа)
- Новый модератор видит кнопки управления

### 2. Истекшая комната
- При попытке входа показывается страница "Room expired"
- Кнопка "Go to home" для создания новой комнаты

### 3. Смена имени
- НЕ предусмотрена
- Как зашел под именем, так и остается

### 4. Дубликаты имен
- Разрешены (не валидируем уникальность)
- Различаются по userId

### 5. Повторное голосование
- Можно менять голос до момента reveal
- После reveal можно менять если модератор снова нажмет "Hide"

---

## UI/UX требования

### Дизайн:
- Минималистичный стиль (типа Vercel/Linear)
- Крупные элементы (кнопки, карточки, текст)
- Достаточно воздуха между элементами
- Responsive (работает на мобилках)

### Темы:
- Light theme (по дефолту)
- Dark theme
- Переключатель в header (доступен везде)
- Настройка сохраняется в localStorage

### Карточки голосования:
- Крупные, квадратные/округлые
- Четко видно значение
- Hover эффект
- Активная карточка выделяется цветом/границей

### Результаты:
- Показываются крупно возле имени участника
- Можно показать как карточку рядом с именем
- Все результаты видны одновременно

---

## API Endpoints (Next.js API Routes)

### `POST /api/room`
Создание новой комнаты
```typescript
Request: { name: string }
Response: { roomId: string, userId: string }
```

### `GET /api/room/[id]`
Получение данных комнаты
```typescript
Query: { userId: string }
Response: Room | { error: "Room not found" }
```

### `POST /api/room/[id]/join`
Вход в комнату
```typescript
Request: { name: string }
Response: { userId: string }
```

### `POST /api/room/[id]/vote`
Голосование
```typescript
Request: { userId: string, vote: string }
Response: { success: boolean }
```

### `POST /api/room/[id]/reveal`
Показать/скрыть результаты (только модератор)
```typescript
Request: { userId: string, revealed: boolean }
Response: { success: boolean }
```

### `POST /api/room/[id]/reset`
Удалить голоса (только модератор)
```typescript
Request: { userId: string }
Response: { success: boolean }
```

---

## Структура проекта

```
story-points-poker/
├── app/
│   ├── page.tsx                      # Главная страница
│   ├── room/
│   │   └── [id]/
│   │       ├── page.tsx              # Страница комнаты
│   │       └── expired/
│   │           └── page.tsx          # Комната истекла
│   ├── api/
│   │   └── room/
│   │       ├── route.ts              # POST создание комнаты
│   │       └── [id]/
│   │           ├── route.ts          # GET данные комнаты
│   │           ├── join/route.ts     # POST вход
│   │           ├── vote/route.ts     # POST голос
│   │           ├── reveal/route.ts   # POST показать/скрыть
│   │           └── reset/route.ts    # POST сброс
│   ├── layout.tsx                    # Root layout
│   └── globals.css
├── components/
│   ├── ui/                           # shadcn/ui компоненты
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   └── ...
│   ├── theme-provider.tsx            # next-themes provider
│   ├── theme-toggle.tsx              # Light/Dark переключатель
│   ├── poker-card.tsx                # Карточка для голосования
│   ├── participant-list.tsx          # Список участников
│   ├── room-controls.tsx             # Кнопки управления (модератор)
│   └── copy-link-button.tsx          # Кнопка копирования ссылки
├── lib/
│   ├── redis.ts                      # Vercel KV клиент
│   ├── types.ts                      # TypeScript типы
│   └── utils.ts                      # shadcn/ui utils
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.js
└── .env.local                        # Vercel KV credentials
```

---

## Deployment на Vercel

### Шаги после локальной разработки:

1. **Создать проект на Vercel:**
   - Зайти на vercel.com
   - Подключить GitHub репозиторий (или задеплоить через CLI)

2. **Настроить Vercel KV:**
   - В Vercel Dashboard → Storage → Create Database → KV
   - Скопировать env переменные
   - Добавить в проект через Settings → Environment Variables

3. **Deploy:**
   - Push в main branch (автодеплой) или `vercel --prod`

4. **Получить домен:**
   - Vercel даст бесплатный домен типа `your-project.vercel.app`
   - Можно добавить кастомный домен если есть

---

## Карточки значений

Стандартный набор Fibonacci + специальные:
- `1` - Очень простая задача
- `2` - Простая задача  
- `3` - Небольшая задача
- `5` - Средняя задача
- `8` - Большая задача
- `13` - Очень большая задача
- `21` - Огромная задача (надо разбивать)
- `?` - Не знаю / Нужно обсудить
- `☕️` - Нужен перерыв

---

## Out of Scope (НЕ делаем в MVP)

❌ История голосований (сохранение прошлых раундов)  
❌ Поле для названия/описания таски  
❌ Статистика (среднее, медиана)  
❌ Кастомные названия комнат  
❌ Смена имени после входа  
❌ Аутентификация / аккаунты  
❌ Сохранение истории комнат  
❌ Экспорт результатов  
❌ Чат в комнате  
❌ Таймер для голосования  

---

## Метрики успеха

✅ Комната создается за < 3 секунды  
✅ Голосование работает без лагов для 8 человек  
✅ UI интуитивно понятный (не нужна инструкция)  
✅ Работает на мобилках  
✅ Нет рекламы и лишних элементов  

---

## Контакты и информация

**Создатель:** Саша (Head of Growth, DiscoverCars.com)  
**Команда:** ~8 человек  
**Частота использования:** раз в 2 недели, ~2 часа  
**Технический уровень:** Computer Science degree, понимает концепты, вайб-кодинг  

**Коммуникация:** Неформально, на "ты", фокус на бизнес-логике  
**Комментарии в коде:** Про бизнес-логику, не про синтаксис  
